#!/bin/bash
# Wait for Pi to be ready
until ping -c1 10.0.20.4 &>/dev/null; do sleep 2; done

# Download keyfile using .netrc credentials
curl -s --netrc-file /boot/config/.netrc -o /root/keyfile "ftp://10.0.20.4/files/keyfile"

# Set permissions
chmod 600 /root/keyfile